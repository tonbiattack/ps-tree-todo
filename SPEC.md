# ps-tree-todo 仕様書

## 1. 概要

`ps-tree-todo` は、PowerShellだけで動作するローカル完結型のツリーTODO管理ツールである。

TODOは親子構造を持つことができ、Windows Forms の `TreeView` を利用してGUI上で階層的に表示・操作する。

データはMarkdownファイルとして保存し、専用DBや外部サービスには依存しない。

## 2. 目的

以下を満たす軽量なTODO管理ツールを提供する。

- PowerShellのみで実行できる
- 追加ランタイムやパッケージマネージャーを必要としない
- Windows標準機能を中心に構成する
- TODOを親・子・孫のように階層化できる
- GUIで直感的に操作できる
- Markdownとして保存し、人間が直接編集できる
- Gitで履歴管理できる
- GitHub Copilot CLI、Codex、Claude CodeなどのAIエージェントからも編集しやすい

## 3. 想定利用環境

- Windows 10 / 11
- Windows PowerShell 5.1 以上
- PowerShell 7 でも可能な限り動作させる
- `System.Windows.Forms` が利用可能な環境

## 4. 基本方針

### 4.1 ローカル完結

ネットワーク通信を必須としない。

TODOデータはローカルファイルに保存する。

### 4.2 Markdownを正とする

アプリ独自DBを持たず、Markdownファイルを永続データとする。

GUIはMarkdownを読み込み、編集結果をMarkdownへ反映する。

### 4.3 PowerShellのみで構成

基本実装は `.ps1` ファイルのみとし、Node.js、Python、Go、.NET SDKなどの追加インストールを要求しない。

GUIにはWindows Formsを利用する。

## 5. TODOデータ形式

初期仕様では、Markdownのチェックリストとインデントで親子関係を表現する。

```markdown
- [ ] 個人開発
  - [ ] シレン6アシスタント
    - [x] 値段識別
    - [ ] READMEを書く
  - [ ] ps-tree-todo
    - [ ] TreeViewを実装
    - [ ] Markdown保存を実装
```

### 5.1 ステータス

- `- [ ]` : 未完了
- `- [x]` : 完了

大文字の `X` も読み込み時には完了として扱ってよい。

### 5.2 階層

- 2スペースを1階層として扱う
- 階層数にアプリ上の固定上限は設けない
- 親タスクと子タスクは同じデータ構造で扱う

### 5.3 タスクID

初期版ではMarkdownの可読性を優先し、必須の内部IDは持たない。

将来的に並び替え・外部連携・AI操作で必要になった場合は、HTMLコメント等によるID付与を検討する。

## 6. GUI仕様

### 6.1 メイン画面

Windows Formsで構築する。

主な構成は以下とする。

- TreeView
- タスク追加ボタン
- 子タスク追加ボタン
- 編集ボタン
- 完了 / 未完了切替ボタン
- 削除ボタン
- 保存ボタン
- 再読み込みボタン

イメージ:

```text
ps-tree-todo

▼ 個人開発
  ▼ シレン6アシスタント
    ☑ 値段識別
    ☐ READMEを書く
  ▼ ps-tree-todo
    ☐ TreeViewを実装
    ☐ Markdown保存を実装

[追加] [子追加] [編集] [完了切替] [削除] [保存]
```

### 6.2 TreeView

`System.Windows.Forms.TreeView` を利用する。

各ノードは1つのTODOを表す。

親ノードは展開・折りたたみ可能とする。

### 6.3 完了表示

完了済みタスクは未完了タスクと視覚的に区別する。

初期版では以下のいずれかで表現する。

- ノード先頭に `☑` / `☐` を表示
- 文字色を変更
- 取り消し線相当の表現

Windows Formsで安定して実装できる方法を優先する。

## 7. 機能要件

### 7.1 タスク追加

選択中のノードと同じ階層に新しいタスクを追加できる。

ルートが未選択の場合はルートタスクとして追加する。

### 7.2 子タスク追加

選択中タスクの直下に子タスクを追加できる。

### 7.3 タスク編集

選択中タスクのタイトルを変更できる。

### 7.4 完了 / 未完了切替

選択中タスクの完了状態を切り替えられる。

初期版では、親タスクを完了しても子タスクを自動完了にはしない。

子タスクがすべて完了した場合でも、親タスクを自動完了にはしない。

自動連動は将来オプションとして検討する。

### 7.5 タスク削除

選択中タスクを削除できる。

子タスクを持つ場合は配下もまとめて削除する。

誤操作防止のため確認ダイアログを表示する。

### 7.6 並び替え

将来的にはドラッグ&ドロップで以下を可能にする。

- 同階層内の並び替え
- 別の親への移動
- ルートへの移動

初期MVPでは後回しでもよい。

### 7.7 保存

現在のTreeViewの状態をMarkdownへ書き戻す。

UTF-8で保存する。

### 7.8 再読み込み

Markdownファイルを再読み込みしてTreeViewを再構築できる。

外部エディタやAIがファイルを更新した場合に利用する。

## 8. ファイル構成案

```text
ps-tree-todo/
├─ ps-tree-todo.ps1
├─ todos.md
├─ SPEC.md
└─ README.md
```

規模が大きくなった場合は以下のように分割する。

```text
src/
├─ Main.ps1
├─ MarkdownParser.ps1
├─ TodoStore.ps1
└─ TodoForm.ps1
```

ただし初期版では、配布・利用の簡単さを優先し、可能であれば単一 `.ps1` に収める。

## 9. 起動方法

想定:

```powershell
powershell -ExecutionPolicy Bypass -File .\ps-tree-todo.ps1
```

PowerShell 7の場合:

```powershell
pwsh .\ps-tree-todo.ps1
```

将来的にはショートカットや `.bat` ランチャーを同梱し、ダブルクリック起動も検討する。

## 10. AIエージェントとの連携

Markdownをデータ形式とすることで、AIエージェントがGUIを経由せず直接TODOを更新できるようにする。

例:

```text
未完了のTODOを確認し、優先度が高いものを1つ実装してください。
完了したら該当TODOを [x] に変更してください。
```

AIによる編集後、ユーザーはGUIで「再読み込み」を実行することで変更を確認できる。

初期版ではファイル監視による自動更新は行わない。

将来的に `FileSystemWatcher` を利用した自動再読み込みを検討する。

## 11. 非機能要件

### 11.1 軽量性

- 常駐プロセスを必須としない
- DBサーバー不要
- 外部API不要
- 起動時にネットワークアクセスしない

### 11.2 可搬性

リポジトリまたはフォルダをコピーするだけで別PCでも利用できることを目指す。

### 11.3 可読性

GUIを使わなくても `todos.md` を読めばTODOの状態を理解できること。

## 12. MVP

最初のリリースでは以下を実装対象とする。

1. `todos.md` の読み込み
2. Markdownチェックリストの解析
3. TreeViewへの親子表示
4. ルートタスク追加
5. 子タスク追加
6. タスク名編集
7. 完了 / 未完了切替
8. タスク削除
9. Markdownへの保存
10. Markdownからの再読み込み

以下はMVP対象外とする。

- ドラッグ&ドロップ
- 期限
- 優先度
- タグ
- 検索
- フィルタ
- 複数TODOファイル切替
- ファイル自動監視
- Git操作
- クラウド同期

## 13. 将来拡張候補

- ドラッグ&ドロップによる階層変更
- キーボードショートカット
- タスク検索
- 完了済みタスクの非表示
- 期限・優先度・タグ
- 複数Markdownファイル管理
- FileSystemWatcherによる自動再読み込み
- CLI操作
- 親タスクの進捗率表示
- 子タスク完了率の自動集計
- Gitコミットとの連携
- AIエージェント向け操作ルールファイル

## 14. コンセプト

本ツールは、高機能なタスク管理サービスを置き換えることを目的としない。

「PowerShellだけで動く」「ローカルファイルだけで完結する」「親子TODOをGUIで扱える」「AIから直接編集できる」という小さく明確な用途に特化する。
